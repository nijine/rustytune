[package]
name = "ecu-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Speeduino serial protocols: TunerStudio Protocol 3 envelope and secondary serial"

[dependencies]
crc32fast = "1"
libc = "0.2"
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
ts-ini = { path = "../ts-ini" }
